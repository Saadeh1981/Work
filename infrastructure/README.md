
# Infrastructure (placeholder)

- App Service (Linux), Python 3.11
- Managed Identity enabled
- Key Vault (optional for MVP; recommended later)
- Azure AI Document Intelligence resource
- SharePoint/OneDrive library for outputs

You can add Bicep/Terraform later; keep first deploy manual to move fast.
